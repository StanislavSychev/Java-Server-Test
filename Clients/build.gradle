plugins {
    id 'application'
}

dependencies {
    implementation(project(":Common"))
}

application {
    mainClassName = 'ru.ifmo.servertest.clients.ClientApp'
}

jar {
    manifest {
        attributes 'Main-Class': 'ru.ifmo.servertest.clients.ClientApp'
    }
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    }
}

run {
    main = 'ru.ifmo.servertest.clients.ClientApp'
    standardInput = System.in
}
